# TODO: F-10 - Refactor Filtering Logic and Population Scoring

**Objective:** Overhaul the filtering logic for both Commune and Bassin de Vie views to use centroid-based distance and geometric intersection for areas. Move population from a filter to a scoring metric.

---

### Group 1: Refactor Population from Filter to Score
- **Status:** Completed
- **Tasks:**
    - [x] **1.1. Remove Population Pre-filter**: In `app/scoring.py`, the population filter block was removed from `compute_odis_score`.
    - [x] **1.2. Update Scoring Configuration**: In `app/scores_config.yaml`, added the new `population_scaled` metric to the `inclusion` category.
    - [x] **1.3. Verification**: The scoring function `add_scores_to_df` is generic and will pick up the new metric automatically.

---

### Group 2: Data Preparation for New Filtering Logic
- **Status:** Completed
- **Tasks:**
    - [x] **2.1. Add Centroid to Communes**: In `app/data_loader.py`, added a `centroid` column to the main `odis` GeoDataFrame.
    - [x] **2.2. Create Bassin de Vie Geo-Dataframe**: In `app/data_loader.py`, created a new cached function `load_bassin_de_vie_geodata()` to produce a GeoDataFrame of BVs with geometries and centroids.
    - [x] **2.3. Add BV Geo-Data to Session State**: In `app/data_loader.py`, updated `init_datasets` to call the new function and add `bv_geo` to the data dictionary.
    - [x] **2.4. Create Area Geo-Dataframe**: In `app/data_loader.py`, created `load_area_geodata()` to produce dissolved département and région polygons and added `area_geo` to the data dictionary.

---

### Group 3: Implement New Filtering Logic
- **Status:** Completed
- **Tasks:**
    - [x] **3.1. Create New Commune Filter Function**: In `app/scoring.py`, created the new `filter_communes()` function.
    - [x] **3.2. Create New Bassin de Vie Filter Function**: In `app/scoring.py`, created the new `filter_bassins_de_vie()` function.
    - [x] **3.3. Refactor `run_search` in `3_Resultats.py`**: Replaced the old `run_search` with a new version that implements the view-aware filtering architecture.
    - [x] **3.4. Refactor `compute_odis_score`**: In `app/scoring.py`, simplified the function to work on pre-filtered data.

---

### Group 4: Testing and Validation
- **Status:** Not Started
- **Tasks:**
    - [ ] **4.1. Update/Create Unit Tests**: Update `test_scoring.py` to reflect the new filtering function signatures and logic.
    - [ ] **4.2. Manual E2E Test**:
        - [ ] Verify the population score appears in the "Inclusion" category details.
        - [ ] Test a distance-based search in the "Communes" view.
        - [ ] Test a distance-based search in the "Bassins de vie" view.
        - [ ] Test an area-based search in the "Communes" view.
        - [ ] Test an area-based search in the "Bassins de vie" view.
        - [ ] Confirm that results seem logical and consistent with the new rules.