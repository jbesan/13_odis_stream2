# Feature F-09: Results by "bassin de vie"

## 🏛️ Constitution Checklist
- [x] **Clarity Over Cleverness**: All new functions and variables will have clear, descriptive names.
- [x] **Write Tests**: A unit test for the aggregation logic will be created.
- [x] **No Magic Numbers**: Column names and other constants will be added to `config.py`.
- [x] **Coherence**: The new feature will integrate with the existing filtering and scoring system.
- [x] **Pandas/NumPy**: Aggregation will use vectorized `groupby()` operations.
- [x] **Type Hints**: All new functions will have full type hinting.
- [x] **Handle Missing Data**: Data merging will be checked for NaNs.
- [x] **Cache Your Data Functions**: The new data loading function will use `@st.cache_data`.
- [x] **Use Widgets as Variables**: The new UI toggle will be assigned to a variable.

---

## 📦 Group 1: Data Loading and Preparation

- [x] **1.1. Update Config**: In `app/config.py`, add new variables for the "bassin de vie" data file path and key column names (`BV_FILENAME`, `BV_CODE_COL`, `BV_NAME_COL`).
- [x] **1.2. Create Loader Function**: In `app/data_loader.py`, create a new function `load_bassin_de_vie_data(file_path)` decorated with `@st.cache_data` to load `csv/insee-bassins-de-vie-2025.csv`.
- [x] **1.3. Integrate Data**: In `app/data_loader.py`, update the main data loading function (`load_all_datasets`) to merge the "bassin de vie" dataframe with the main communes dataframe. Ensure the merge is clean and handles any potential missing values.

---

## 🧪 Group 2: Backend - Scoring and Aggregation Logic

- [x] **2.1. Create Aggregation Function**: In `app/scoring.py`, create a new function `aggregate_scores_by_bassin_de_vie(df: pd.DataFrame) -> pd.DataFrame`. This function will:
    - Group the scored commune dataframe by the "bassin de vie" ID.
    - Aggregate the scores. A weighted average by population (`sum(score * pop) / sum(pop)`) should be used for numeric scores.
    - Return a new dataframe with one row per "bassin de vie", containing the aggregated scores.
- [x] **2.2. Create Geometry Aggregation**: In `app/maps.py`, create a function `dissolve_communes_to_bassins_de_vie(gdf: gpd.GeoDataFrame) -> gpd.GeoDataFrame` that uses the `dissolve()` method to merge commune geometries into "bassin de vie" polygons.

---

## 🎨 Group 3: Frontend - UI and Visualization

- [x] **3.1. Add UI Toggle**: In `app/3_Resultats.py`, add a toggle to the sidebar to let the user choose the view. `st.session_state['view_level'] = st.radio("Niveau d'agrégation", ["Communes", "Bassins de vie"])`.
- [x] **3.2. Conditional Logic**: In `app/3_Resultats.py`, add an `if/else` block based on `st.session_state['view_level']`.
    - If "Communes", run the existing scoring and display logic.
    - If "Bassins de vie", call the new aggregation function `aggregate_scores_by_bassin_de_vie` on the scored commune data.
- [x] **3.3. Update Results List**: In `app/ui.py`, modify `display_results_list` to accept either a commune or a "bassin de vie" dataframe and display the information accordingly (e.g., show "Bassin de vie de [Name]" instead of commune name).
- [x] **3.4. Update Map**: In `app/maps.py`, modify `create_map` to handle the aggregated GeoDataFrame. It should:
    - Use the dissolved "bassin de vie" geometries.
    - Color the choropleth based on the aggregated scores.
    - Adjust tooltips to show "bassin de vie" information.

---

## ✅ Group 4: Testing and Validation

- [x] **4.1. Write Unit Test**: In `app/tests/test_scoring.py`, create a new test `test_aggregate_scores_by_bassin_de_vie`.
    - Create a small, sample `pd.DataFrame` of communes with scores, populations, and "bassin de vie" IDs.
    - Call the `aggregate_scores_by_bassin_de_vie` function.
    - Use `assert` to verify that the aggregated score is calculated correctly.
- [x] **4.2. Manual E2E Test**: After implementation, run the Streamlit app.
    - Verify the "Bassins de vie" toggle appears and works.
    - Check that the results list and map update correctly when the toggle is switched.
    - Ensure that filtering and weight adjustments still work in the new view.
- [ ] **4.3. Run Full Test Suite**: Run `pytest app/tests/` to ensure no existing functionality has been broken.